RewriteEngine On
RewriteBase /
RewriteCond %{REQUEST_URI} !(dispatch.fcgi)
RewriteRule ^(.*)$ dispatch.fcgi/$1 [L]

ErrorDocument 500 "<h2>Application error</h2>Python FCGI application failed to start properly"
